import React, { useState } from "react";
import styled from "styled-components";

const NavBar = styled.nav`
  width: 100%;
  height: 10vh;
  display: flex;
  justify-content: center;
  align-items: center;
  background: #182421;
  border-radius: 40px 40px 0px 0px;
`;

const Header = () => {
  return <NavBar>sdadsa</NavBar>;
};

export default Header;
